<html>
  <body>
    <center><h1>Editer mon event</h1></center>
    <div class="container col-md-8 order-md-1">
    <%= form_for @event do |f| %>
    <div class="form-group">
        <label>Title</label>
        <%= f.text_field :title, class: "form-control", placeholder: @event.title %>
        <br />
    </div>
    <div class="form-group">
        <label>Description</label>
        <%= f.text_area :description, class: "form-control", placeholder: @event.description %>
        <br />
      </div>
      <div class="form-group">
        <label>Start date</label>
        <%= f.datetime_field :start_date, class: "form-control", placeholder: @event.start_date %>
        <br />
      </div>
      <div class="form-group">
        <label>duration</label>
        <%= f.text_field :duration, class: "form-control", placeholder: @event.duration %>
        <br />
      </div>
      <div class="form-group">
        <label>Prix</label>
        <%= f.text_field :price, class: "form-control", placeholder: @event.price %>
        <br />
      </div>
      <div class="form-group">
        <label>Location</label>
        <%= f.text_field :description, class: "form-control", placeholder: @event.location %>
        <br />
      </div>
        <center>
        <button type="submit" class="btn btn-primary">Éditer mon évènement</button>
        </center>
        <br />
      <center>
        <a href="<%= event_path(@event.id)%>" class= "btn btn-danger" data-confirm= "Êtes-vous sûr(e) de vouloir supprimer votre évènement?" data-method="DELETE">Supprimer mon évènement</a>
        </center>

    <% end %>
    <br />
    <% if @event.errors.any? %>
  <p>Nous n'avons pas réussi à éditer l'évènement pour la (ou les) raison(s) suivante(s) :</p>
  <ul>
    <% @event.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
  <% end %>
  </div>

  </body>
</html>
